# Frontend build-time config (Vite). For local dev you can leave this unset (defaults to localhost:8000).
# In production (Vercel), set VITE_API_BASE to your deployed backend URL, e.g. https://carbondesk-api.up.railway.app
VITE_API_BASE=http://localhost:8000
